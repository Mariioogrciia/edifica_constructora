# =============================================================================
# Edifica Constructora - Dependencias del Backend e IA Consolidadas
# Instalar con: pip install -r requirements.txt
# =============================================================================

# ─── BACKEND (FastAPI / Servidor / Base de datos) ───
fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
aiosqlite==0.20.0
python-multipart==0.0.12
pydantic==2.9.2

# ─── DETECTOR IA & PROCESAMIENTO (YOLO / OpenCV) ───
ultralytics==8.3.2
opencv-python==4.10.0.84
requests==2.32.3
numpy==1.26.4
lapx>=0.5.2

# ─── UTILIDADES DE DATOS (Descargas de Dataset) ───
datasets>=2.16.0
huggingface-hub>=0.20.0
